#!/usr/bin/env bash
# Don't use `set -e`

alias is_symlink="test -L"
alias list="ls -halt"
alias gtask="task -t ~/Taskfile.global.yml"
alias npxg="npx_exec_global"

# Git
alias git-search="git log -p -G"
alias gca="git commit --amend"
alias gcan="git commit --amend --no-edit"
alias gl="git log"
alias git-commit-skip-hooks="git commit --no-verify"
# 3rd party aliases (provided by oh-my-zsh)
#	https://github.com/ohmyzsh/ohmyzsh/blob/master/plugins/git/git.plugin.zsh
#	gd = git diff
#	gds = git diff --staged
#	... many more

# Work
alias work-time="asdf_exec_with_global_version npm exec -- jttu harvest rollup --target-user=just-me --target-week=this"
